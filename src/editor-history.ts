import * as vscode from 'vscode';

/*export function registerHistory(context: ExtensionContext) {
  vscode.window.onDidChangeTextEditorOptions(o => {
    if (o.textEditor === o.textEditor) {

    }
  });
}*/
